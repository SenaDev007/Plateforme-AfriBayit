{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npx turbo run build --filter=@afribayit/web",
  "outputDirectory": ".next",
  "installCommand": "npm ci",
  "regions": ["cdg1", "fra1"],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
